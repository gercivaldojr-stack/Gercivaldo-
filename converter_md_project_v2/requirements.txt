# Dependências obrigatórias
pymupdf>=1.23.0
python-docx>=1.0.0
chardet>=5.0.0

# Interface web (opcional - apenas se usar streamlit run app/main.py)
streamlit>=1.28.0

# Testes
pytest>=7.4.0

# Configuração YAML (opcional - apenas se usar --config)
pyyaml>=6.0

# OCR seletivo (opcional na CLI - requer Tesseract no sistema)
# O Dockerfile já instala tesseract-ocr e tesseract-ocr-por
pytesseract>=0.3.10
Pillow>=10.0.0
